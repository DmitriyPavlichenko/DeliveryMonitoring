server:
  error:
    include-message: always
    include-binding-errors: always

logging:
  level:
    org:
      springframework:
        security: DEBUG

spring:
  datasource:
    url: jdbc:postgresql://ec2-54-76-249-45.eu-west-1.compute.amazonaws.com:5432/d11emiks2ufkvo
    username: username
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true