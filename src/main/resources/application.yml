server:
  error:
    include-message: always
    include-binding-errors: always

logging:
  level:
    org:
      springframework:
        security: DEBUG

spring:
  datasource:
     url: jdbc:postgresql://ec2-54-217-232-239.eu-west-1.compute.amazonaws.com:5432/de360p7ca4kjn9
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true